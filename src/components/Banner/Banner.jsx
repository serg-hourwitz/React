import styles from './Banner.module.css';


const Banner = () => {
  return (
    <div className={styles.banner}>
      <span className={styles.title}>Full-width banner image</span>
    </div>
  );
}

export default Banner